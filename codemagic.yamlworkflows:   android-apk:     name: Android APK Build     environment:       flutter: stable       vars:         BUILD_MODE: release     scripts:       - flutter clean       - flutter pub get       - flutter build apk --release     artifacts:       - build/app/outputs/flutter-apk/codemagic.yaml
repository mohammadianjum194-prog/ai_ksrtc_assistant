workflows:
  android-apk:
    name: Android APK Build
    environment:
      flutter: stable
      vars:
        BUILD_MODE: release
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
